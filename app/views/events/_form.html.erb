<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :バンド名 %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :代表者 %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :日付・時限 %>
    <%= form.date_select(:start_date,
        use_month_numbers: true,
        date_separator: '-',
        default: Date.today)
    %>
    <%= form.select('s1', options_for_select({"1" => "08:50", "2" => "09:30", "2'" => "12:00", "3" => "13:00",
    "4" => "14:40", "5" => "16:20" , "6" => "18:00", "7" => "19:40", "7'" => "21:10"})) %><%= '限' %>
  </div>

<%#    <div class="field">                                    %>
<%#   <%= form.label :end_date                                %>
<%#   <%= form.datetime_select :end_date, {discard_hour:true} %>
<%#  </div>                                                   %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
